// Codes by mahdi tasha
// Importing part
import type { NextConfig } from "next";

// Defining the configs of next.js
const nextConfig: NextConfig = {};

// Exporting the configs as default
export default nextConfig;
